{
  "name": "modak-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:ci": "jest --ci --watchAll=false",
    "test:debug": "jest --verbose --no-cache",
    "test:headless": "HEADLESS=true jest",
    "test:parallel": "PARALLEL=true jest --maxWorkers=4",
    "build": "tsc",
    "report:open": "open reports/jest-report.html",
    "qa:setup": "mkdir -p screenshots reports",
    "qa:clean": "rm -rf screenshots reports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.8.1",
    "jest": "^30.2.0",
    "jest-html-reporter": "^4.3.0",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "puppeteer": "^24.25.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
