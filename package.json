{
  "name": "modak-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "dotenv -e .env -- npx playwright test",
    "test:headed": "dotenv -e .env -- npx playwright test --headed",
    "codegen": "dotenv -e .env -- npx playwright codegen $BASE_URL"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "dotenv-cli": "^10.0.0"
  }
}
